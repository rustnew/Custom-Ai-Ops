apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bjw-template.fullname" . }}-config
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "bjw-template.labels" . | nindent 4 }}
data:
  ENGINE_TYPE: "vllm"
  MODEL_NAME: {{ .Values.model.name | quote }}
  VLLM_HOST: "0.0.0.0"
  VLLM_PORT: {{ .Values.service.targetPort | quote }}